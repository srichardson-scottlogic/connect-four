const minimax = (node, depth, maximisingPlayer) => {
    if(depth = 0
        || ){

    }

    if(maximisingPlayer){
        let value = -Infinity;
        for e
    }
}


export default { minimax };